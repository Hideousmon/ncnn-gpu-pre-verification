name: container test
on:
  push:
    branches: [main]

jobs:
  windows-build:
    name: windows image test
    runs-on: ubuntu-latest
    steps:
      - name: test hello world
        run: |
          docker run hello-world
