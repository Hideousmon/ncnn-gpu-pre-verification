name: container test
on:
  push:
    branches: [main]

jobs:
  windows-build:
    name: windows image test
    runs-on: ubuntu-latest
    container:
      image: dockcross/windows-x86
    steps:
      - name: ls test
        shell: bash
        run: |
          ls