name: container test
on:
  push:
    branches: [main]

jobs:
  windows-build:
    name: windows image test
    runs-on: ubuntu-latest
    steps:
      - name: test hello world
        run: |
          docker pull dockcross/windows-shared-x86
          docker images
          docker run -it dockcross/windows-shared-x86 powershell
          